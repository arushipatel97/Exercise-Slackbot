version: '2.0'

services:
  exercise-slackbot:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/go/src/github.com/rapidloop/exercise-slackbot
    environment:
      SLACK_TOKEN:
      OAUTH_ACCESS:
      CHANNEL: 
